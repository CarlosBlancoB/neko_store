# Handoff NEKO - 2026-06-11

## Regla de producto

- NEKO no es un POC.
- No existe categoria `demo`.
- Los productos iniciales/staging se tratan como productos reales en UI.
- No mostrar badges, textos ni labels `DEMO` en catalogo, cards, carrito, admin o checkout.
- Los IDs tecnicos `demo-*` solo sirven para limpieza interna pre-produccion.

## Hecho en este tramo

- Formulario real de contacto conectado a PostgreSQL.
- Bandeja admin `Mensajes` para ver, filtrar y actualizar consultas.
- Spec `_bmad/specs/11-contact-messages.md`.
- Imagen de Nosotros copiada a `public/brand/nosotros.png`.
- Imagen de Contacto copiada a `public/brand/contacto.png`.
- CMS empezado para imagen/texto de Nosotros y fondo opcional de Contacto.
- Migraciones nuevas:
  - `019_create_contact_messages.sql`
  - `020_about_content_and_home_featured.sql`
  - `021_remove_visible_demo_product_flags.sql`
- Home deja de mostrar empty state de categoria cuando no hay destacados.
- PWA home cambia tabla comparativa por CTA/leyenda.
- Imagenes en cajas principales empiezan a anclarse arriba con `object-position: top center`.
- Seed minimo de catalogo ya no usa badge visible `DEMO`.

## Pendiente activo

### P0 - Auth/Admin

- Admin debe poder entrar con la cuenta admin real unica.
- Admin login debe tener boton de mostrar/ocultar contrasena.
- Boton `Cambiar numero` y cualquier boton similar debe tener styling consistente.
- OTP WhatsApp no esta llegando; revisar flujo real de envio y fallback admin.
- 2FA debe integrarse sin bloquear incorrectamente la unica cuenta admin.
- Password admin se debe poder cambiar dentro del dashboard protegido con 2FA.

### P0 - Checkout/SINPE

- `Confirmar Pedido` debe exigir numero completo de comprobante y/o captura.
- Direccion debe ser opcional; cliente puede completarla luego o desde dashboard.
- No se crean reservas sin dinero.
- Boton principal de comprobante debe estar deshabilitado hasta que haya captura y/o numero.
- Si falta comprobante, mostrar mensaje claro y opcion alternativa para hablar por WhatsApp.
- Admin revisa y aprueba comprobante; solo entonces se procede con preparacion/stock/orden.
- Eliminar simbolos `$` restantes en carrito/checkout/admin; todo debe usar colones `₡`.

### P0 - Datos/Sync

- Tienda y carrito deben persistir localStorage como cache local.
- Si el usuario esta logueado, carrito/perfil/ordenes deben sincronizar contra DB.
- Zustand es cache/UI; mutaciones operativas siempre por API.
- Completar DATA-001 antes de produccion: borrar IDs `demo-*`, cuenta Valentina, ordenes/notificaciones de prueba y seeds de staging.

### P1 - Home/CMS/WordPress Style

- Nosotros debe usar imagen `nosotros.png` desde CMS/DB, editable en dashboard.
- Contacto debe tener fondo parallax opcional para forms:
  - default apagado;
  - si se activa, usar `/brand/contacto.png` como imagen ya cargada;
  - permitir subir imagen custom desde dashboard.
- Home destacados estilo WooCommerce:
  - marcar producto como destacado desde admin;
  - ordenar destacados desde admin;
  - Home muestra solo esos productos y respeta orden.
- Terminar UI de `ProductManager` para `featured` y `featured_sort_order`.
- PWA dentro del home necesita padding lateral correcto.
- Logo en PWA debe cambiar segun dark/light theme.
- Favicon debe ser el gato/logo del sitio, no el texto.

### P1 - UI/Responsive

- Usar skeletons en productos mientras cargan datos e imagenes.
- Producto sin imagen debe mostrar placeholder con logo gato + texto NEKO.
- Imagenes dentro de cajones deben poder acomodarse; por defecto usar top center.
- En responsive, el recorte debe bajar desde la parte superior, no centrarse.
- No dejar controles sin styling en ninguna pagina.

### P1 - Shipping

- Revisar politica/envios:
  - Ruta
  - Primer kg
  - Kg adicional
  - GAM a GAM
  - Tarifas aproximadas
- Redondeo hacia arriba debe ser a numeros cerrados de 500 en 500 o al mil cercano segun caso:
  - ejemplo: 2640 -> 3000;
  - no usar valores como 3380.

## Validaciones recientes

Antes de los ultimos cambios parciales pasaron:

- `npx.cmd biome check src/`
- `npm.cmd run typecheck`
- `npm.cmd test`
- `npm.cmd run build`

Despues de retomar, volver a correr todos porque hay cambios parciales en CMS/ProductManager.

## Notas para el proximo chat

- No continuar agregando features visuales sin cerrar auth/admin/checkout.
- No tratar seeds como demo visible.
- Priorizar que todo lo editable por negocio viva en dashboard + DB.
- Excluir de git: `.env`, logs, `coverage/`, `dist/`, `tsconfig.tsbuildinfo`.
